/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'media.discordapp.net',
			'cdn.discordapp.com',
			'cdn.midjourney.com',
		],
	},
};

module.exports = nextConfig;
